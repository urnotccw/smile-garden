# 微笑花园：当前视觉与交互规范

模式：Experience。人物是主角；统一纸面蜡笔纹理，柔和、童话感，避免强光遮脸。使用已有生成素材，不扩张新的玩法。

雨景：浅蓝实心/空心雨滴、长短白色拖尾、手绘涟漪；绿色蜡笔草地高 16%。花草高低/透明度不同，最高类已缩小约 15%，最多 36 株随宽度调整；中央用低株，两侧可高，固定根部等比例生长 2.1–3.1 秒，无纵向压扁或硬切。

烟花：星球、星环、爱心、鲸鱼四种图案，完整图案须处于屏幕内且下沿高于头顶安全线。三档半径系数 .245/.13/.17，再按可用空间限制；中心位于头顶空间 .48–.66。最多三朵，成对升空错开 .9 秒，形成约 1.05 秒后散开。各图案两种主色加奶油高光；脸部前方普通装饰减弱，头顶接触痕迹和反弹爱心保留清楚。

烟花三层：底层为同色系深色蜡笔烟云，在图案形成时渐显，限制在头顶上方；主体为沿现有几何轮廓绘制的双色蜡笔色块，带局部偏移暗边、纸纹和亮笔触；顶层保留轮廓主粒子，降低辅助亮点和拖尾透明度。主体与粒子使用同一 .8 秒展开曲线，在 1.05 秒炸开时于 .22 秒内整图淡出，不再切成色片；由真实物理粒子自然散开并下落。四种主体全局缓存，三枚事件上限，移除碎片缓存与绘制，各画质档位均快速淡出。雨滴、反弹爱心和星星的缓存素材加入少量底部深色颜料边和上部纸色亮边，不改变尺寸与碰撞。

水面：烟花真正落水后才显示底部 7.5% 浅蓝蜡笔色带；.55 秒显现，最后落水后保持 2.6 秒，再 .9 秒淡出。星星、水花、涟漪在其上方。

烟花下落密度：完整绽放仍使用全部轮廓点；炸开后约 60% 点在 .45 秒内淡出，仅 40% 继续下落与碰撞。保留点的尺寸在 .35 秒内平滑过渡为小／中／大三档（半径 1.6–2.3／3.3–4.1／5.6–6.6，手机再乘 .9），比例约 50%／40%／10%。短暂消散的轮廓点不参与接住、碰头或恢复下雨计数；降雨恢复继续按实际下落点完成 60% 计算。

表情：微笑阈值默认 10%，可手动调节 5%–90% 并保存在本机；仪表阈值线与百分比同步，调节后重新确认触发，低阈值的退出区间按比例限制，避免零表情被当作持续微笑。大笑默认 60% 并使用双侧嘴角/张嘴分数。短时防抖，持续大笑优先停雨、花园 1 秒淡出；退出大笑后，每枚烟花需完成约 60% 主体粒子飞行才恢复下雨，升空中仍阻塞。爱心/星星转换、落水及失效离场均视为主体飞行完成，避免残留永久阻塞。恢复下雨时旧普通星光/烟雾降低亮度，碰撞继续。

界面：不恢复删除的参数面板。欢迎页讲清微笑/大笑及本地处理，提供摄像头和演示入口；正常隐藏 FPS。debug 查询显示真实分项指标，safe 查询仅用于直播安全区验收。降低动态效果时隐藏爆炸闪光、减少烟雾光晕与闪烁。

适配：桌面与手机保持人物清晰，尊重安全区和触控；静态图案边界验证与手机摄像头实测均需要。技术与验收数据以 README、验证记录为准，历史设计说明移至 docs/history。

手机预览：右侧开关切换 iPhone 15 Pro 的 393:852 屏幕比例（对应 1179:2556），钛灰手机边框、圆角与灵动岛。顶部头像、底部评论/操作区为不可交互的直播示意，文字提示未开播。切换实际画布尺寸和 cover 坐标，保留镜像设置；纯净画面保留比例并隐藏模拟 UI。手机雨滴/拖尾 alpha .66，密度倍率 .62，最短拖尾底限由 3.6 倍雨滴尺寸调整为 4.6 倍，短档系数下限 .48。桌面参数不变。截图所示“画面待机 / 演示降雨 / 重种花园”工具栏已删除。

竖屏花草：最高类别（高度范围上限超过 .3）显示尺寸再乘 .8。前后两层，新生长与可见株数上限 10，按实际宽度与花型降低；后排根部 .926–.94、尺寸再乘 .82、透明度再乘 .74，前排根部 .978–.992。按根部位置从后往前绘制，碰撞使用同一根部与尺寸。成熟插画的同层宽度留距，跨层间距放松并横向错开，允许适度遮挡。切入竖屏时从已有花园选择分布合适的组合，切回电脑恢复完整花园；不修改原始插画或桌面尺寸。

竖屏种植分布：雨水滋养整片草地后优先补齐视觉重量较小的左、中、右区域，而非仅在落雨位置重复增大已有植株。两侧首次选用中等或高株花，中央选用较低花草；先尝试完整花型，空间不足再用小株补空。沿用成熟宽度间距及大株 .8 倍尺寸。电脑花园切入时先从三个区域选择代表花草，再填补空位，避免小叶片排挤整株花或两株大花挤掉中间区域。

默认画幅：初始 HTML 和运行状态均为 iPhone 15 Pro 竖屏，避免初载闪现横屏。右侧以“手机竖屏 / 电脑横屏”单选切换，横屏实际比例为 16:9；纯净画面保留既有退出方式。微笑强度为实时读数，下面的触发阈值为用户设置，二者分开显示。

手机雨滴与爱心落水：雨滴出生尺寸为原尺寸的 .8；最短拖尾下限改为缩小后尺寸的 5.75 倍，保持原有绝对长度。竖屏开启爱心落水转换，只有下降且跨过水线才转成蜡笔星星，先响应水花/涟漪再跃出星星，沿用大小分档和蓝色水面显现。水线附近手掌接住优先，不重复生成水面星星；电脑横屏的爱心落水规则保持原行为。

笑意绽放：微笑感应下方新增独立的大笑感应区，淡紫色仪表呼应烟花；显示真实大笑强度、等待人脸/张嘴笑一笑/烟花绽放中/星光缓缓落下状态。烟花阈值默认 60%，手动范围 50%–90%，本地持久化；调整后重新确认表情，保留发射冷却。与微笑阈值互不修改，无脸或关闭摄像头显示“—”，降雨演示不伪造大笑强度。

电脑右侧一屏控制：宽度超过 760px 时压缩各区间距，主要按钮 36px、读数 26px，保留全部画幅/镜头/感应/纯净画面操作，不使用整体缩放或内嵌滚动条。高度不超过 700px 时再收紧间距并收起重复说明；滑杆解释保留在悬停标题和无障碍描述中。手机窄屏保持原有触控布局。

1.4 评审优化：手机直播示意的评论与操作区后，植物／烟花装饰通过缓存软遮罩局部减弱，纯净画面关闭；不改变根部、手部或粒子碰撞。主轮廓粒径提高约 12%，弱装饰缩小，星球内部线和外围卫星点减少；头顶接触提示最后绘制。镜头状态区区分授权、模型准备、首帧识别与就绪。各画布共用三级 DPR 上限，保留蜡笔纹理。状态、诊断、调度和验收证据以 1.4 README 与验证记录为准。
