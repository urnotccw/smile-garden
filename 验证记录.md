# 当前验证记录 · 2026-09-16

## 本次提交核验

交互版本：`a0c52d3f6b64361b20754af99abb735345c9dff4`，Pages 成功运行：https://github.com/urnotccw/smile-garden/actions/runs/34871016595 。本次仅同步提交文档，未修改该交互版本。

- 匿名 HTTP：仓库 `private=false`、`visibility=public`；首页及 app.js、effects.js、fireworks.js、firework-ink.js 均为 200，与本地 dist 内容哈希一致。
- 108 项规则测试通过，包括持续生长、单组新旧交替、数量上限、微笑停止和烟花抑制。
- 浏览器 acceptance、release-recovery、render-budget、tracking-response、startup-recovery 全部通过。acceptance 覆盖 1366×768、1280×600、390×844 布局，Worker 与主线程真实模型启动、模拟权限拒绝及恢复。
- 当前成本复核：竖屏/横屏草地缓存 54,234 / 111,100 像素，软遮罩 12,800 像素；无摄像头待机 1.5 秒，三个特效画布 clearRect / drawImage 均为 0。Chrome 152，DPR 1。旧值与范围见 ROI 记录。
- 测试使用桌面浏览器、合成视频与受控识别结果；没有把模拟手机框当作手机真机，也不据此承诺真人准确率、全平台兼容或温升表现。

## 1.5 发布恢复记录（2026-09-14）

日期：2026-09-14。版本发布状态以 GitHub Actions 最新成功运行及对应提交为准。Demo：https://urnotccw.github.io/smile-garden/ ，Public 源码：https://github.com/urnotccw/smile-garden 。

本轮 104 项 Node 规则测试通过。新增素材有界重试、超时及旧回调失效、视频暂停/停帧与丢脸的区分。原有头部碰撞、手掌接取、60% 主体飞行完成后接雨、10/60 FPS 花草生长和粒子上限测试继续保留。

浏览器使用 Windows / Chrome / Playwright、本地服务和合成画布摄像头；真实 MediaPipe 模型参与启动与故障恢复检查。受控表情结果用于验证触发时序。不代表微信真机、实时真人或直播编码负载测试。

本轮故障回归：图片 5.2 秒后失败可显示首屏状态；每个失败图集最多三次请求后停止；模拟网络恢复后只补载失败图集；图片就绪后继续使用原素材。手部模型失败不会再记录 handReady 成功时间。视频暂停自动恢复，播放被拒绝后可在预览内手动恢复；用户关闭相机后页面返回不会重新开启。

发布流程增加锁定依赖和 Chromium 检查，运行 release-recovery、render-budget、tracking-response，全部成功才允许构建与 Pages 部署；不发布测试依赖与测试结果。失败时保留线上上一版。

复现：npm ci；npx playwright install chromium；npm test；npm run test:browser；npm run test:recovery；npm run test:render；npm run test:tracking；npm run test:startup；npm run build。BROWSER_PATH / PLAYWRIGHT_MODULE 可指定已有测试浏览器与共享运行时。

ROI 的局部缓存与绘制节省见 docs/ROI优化-2026-09-14.md；启动及生长的历史测量有明确场景边界。之前的阶段记录已归档到 docs/history/验证记录-1.4.0-发布修复前.md，不作为当前交付状态。

仍待实际设备验收：iPhone Safari、Android Chrome、微信内置浏览器、3–5 位真人、10 分钟发热与真实摄像头中断。结果填写 真机验收表.md，不能用合成流代填通过。
