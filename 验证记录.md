# 验证记录 · 1.4.0

日期：2026-09-13。本机 Windows / Chrome headless / Playwright；测试图片生成摄像头流，真实 MediaPipe 参与。不是手机真机或实时真人验收。旧记录保存在 docs/history/验证记录-1.3.0-整理前.md，不作为当前参数依据。

## 已通过

- Node 规则测试 76/76：60% 主体飞行完成门槛、新烟花阻塞、雨景退出、头顶连续碰撞、手掌接取、竖屏爱心落水、粒子上限、五分钟模拟时间排空、花草生长、识别调度、分项诊断及 Worker 先返回脸后返回手。
- 桌面 1366×768、1280×600 和手机模拟 390×844：默认竖屏、16:9 切换、阈值调节和保存、花草布局缓存、纯净画面与 Escape 退出，无页面脚本异常和横向溢出；两个桌面高度右侧全部控件仍在一页内。
- 权限拒绝后重试；延迟模型请求时可见“正在准备识别”；Worker / 主线程两种路径均就绪；人像离场后解除人脸状态；模型请求失败提示重试，关闭可恢复未开启状态。
- 降低动态效果设置被识别；图片流有手部检测时保持快速采样，切到纯色无手流后两路径均约 6Hz 探测。
- 检查手机花草／评论区实际素材截图及四种烟花图案图集；主轮廓清楚，评论后装饰减弱，纯净画面不使用安全区遮罩。图集不是实时真人碰撞录像。
- 运行构建生成 dist，未部署。

## 性能采样（本机短窗口，不能外推真机）

| 路径 | 绘制 fps | 脸 Hz | 手 Hz | 无手探测 Hz | 脸延迟 p95 ms | 手延迟 p95 ms |
|---|---:|---:|---:|---:|---:|---:|
| worker | 56 | 7 | 18 | 6 | 147.4 | 61.8 |
| main | 38 | 7 | 13 | 6 | 43 | 80.9 |

数据见 docs/verification-1.4.0.json。首次模型推理包含初始化／编译，主线程仍观察到约 3 秒首帧等待；加载状态现区分模型准备、首帧识别及未发现人脸。主线程同步计算仍可能掉帧，不能宣称所有手机达到稳定 30fps。

测试使用请求拦截来验证延迟和失败，会影响浏览器缓存；资源字节不是公网冷启动结论。Worker 的资源记录来自 Worker 自身，主线程来自页面。线上弱网／冷缓存／热缓存需独立测试。

## 可复现

npm test；npm run test:browser；npm run build。浏览器脚本位于 tests/browser/acceptance.mjs，运行方式见 README。没有将个人图片、机器绝对路径写入测试默认值。素材图、代理轮廓验证使用开发入口，不伪造运行页面的表情。

## 保留待验收

2026-09-13 烟花分层补充（按反馈移除炸开色片）：78 项规则测试通过，静态构建通过。Headless Chrome 检查四种图案的绽放／快速淡出后粒子散开、393×852 竖屏和 960×540 横屏绘制、减少动态效果、事件清空及到期释放，未发现页面异常。炸开 .22 秒后主体完全消失，没有分片几何或碎片缓存；四种主体位图共 2.25 MiB（不含 GPU 副本和其他素材）。预热后连续 120 次绘制未创建新 canvas。过渡连续性与生命周期测试见 tests/firework-layers.test.mjs。此轮为合成场景渲染验证，没有重新做真人摄像头或手机硬件验收；新增视觉尚未同步到托管网站。

真实 iPhone Safari / Android Chrome、3–5 位真人、摄像头硬件占用和切镜头、暗光与遮挡、真实连续十分钟发热、直播编码负载、公网 HTTPS 与 Public 仓库。填写真机验收表.md，当前不得标为通过。发布仍暂停。

下落粒子调整：79 项规则测试与静态构建通过。保留完整绽放图案，之后约 40% 粒子继续下落，尺寸分三档；新增集成测试确认消散轮廓点不会提前释放降雨。合成浏览器场景无页面异常，尚未更新线上版本。

## GitHub 正式发布（2026-09-13，更新上述发布状态）

源码已公开到 https://github.com/urnotccw/smile-garden ，首次完整提交 e82464a3eafcca102ab98e6c86dc440ff85dc743 的 88 个文件与本地上传包逐一哈希一致。GitHub Actions 的 79 项规则测试、构建及 Pages 部署通过。正式地址：https://urnotccw.github.io/smile-garden/ 。以上“发布暂停／尚未同步”的记录属于此前阶段；本轮最新视觉已在 Pages 发布，旧 Sites 地址仍为旧版。

公网首页、分层脚本、脸／手模型、WASM 返回 HTTP 200；WASM MIME 为 application/wasm。Headless Chrome 在实际 Pages 地址验证 HTTPS 安全上下文、默认竖屏、横屏切换、390px 手机布局无横向溢出、Worker 与手部模型初始化；无页面异常或失败资源。摄像头输入为合成画布视频流，未操作真人摄像头，不替代 iPhone／Android 真机和真实直播负载验收。
