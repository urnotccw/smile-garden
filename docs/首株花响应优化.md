# 首株花响应（2026-09-14）

草地显现到 30% 后即可播种，不再等待 4.5 秒完全铺开。真实雨滴落地，涟漪展开 0.3 秒后触发生长；较早落地的位置仍进入有限队列，等草地初步显现后逐渐释放。仅有草地、雨滴被手接住、烟花期间均不会凭空长花。

首株优先使用可辨认的中等花朵，提高可见度，生长时间为 1.45–1.85 秒且不额外延迟；其余花草保留原有高低、透明度、前后关系和 2.1–3.1 秒的生长节奏。草地颜色、高度及花草数量上限保持不变。

花园按实际可见帧间时间推进，每个物理子步不超过 50ms、最多补进 0.5 秒，避免低帧率把下落与生长拖成慢动作。长后台间隔不会积累无限补帧。头部烟花规则和 60% 落地后的降雨衔接不变。

清理已删除的 grow 开关：不再读取或保存历史 grow:false，应用中的花草生长始终启用；其他用户阈值保留。两个植物图集独立判断是否可绘制，避免其中一个尚未下载完时阻塞另一个。

98 项自动测试通过，覆盖真实落地前不长花、10/60 FPS 时序差异、草地初显后释放种子、手掌接雨、烟花清理和独立图集绘制。浏览器回归验证旧设置迁移、手机与桌面布局、模型加载及摄像头表情触发流程。静态构建通过。

固定随机序列的合成场景中，手机画幅首株约在降雨后 2.42 秒开始生长、桌面约 1.97 秒；四秒画面已检查可见花朵。以上时间从模拟降雨开始计算，不包含网络下载和真人表情识别，不能当作微信真机响应承诺。
